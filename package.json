{
  "name": "mini-trello",
  "private": true,
  "scripts": {
    "build-all": "concurrently \"npm --prefix fe run build\" \"npm --prefix be run build\"",
    "dev-all": "concurrently \"npm --prefix fe run dev\" \"npm --prefix be run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
